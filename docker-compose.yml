version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio-2026:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped

